<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <form>
      <button type="menu" href="/new">Create Message</button>
    </form>
    <ul>
      <% messages.forEach((message)=>{%>
      <li>
        <ul>
          <p><%= message.text%></p>
          <p><%= message.user%></p>
          <p><%= message.added%></p>
        </ul>
      </li>
      <%})%>
    </ul>
  </body>
</html>
